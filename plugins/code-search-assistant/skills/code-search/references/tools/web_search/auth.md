# web_search — Auth

## No credentials

Builtin `web_search` requires no MCP `authorization` token and no API key in the tool-config — it is a platform builtin, not an MCP server. There is no secret to reference for this tool.

# No "Best Dataset" — Metadata Proxy Only

- Use "**candidate datasets**," never "best dataset."
- Ordering/fitness is explicitly a **metadata proxy**, not a scientific conclusion — state this.
- Prefer **HLSP / high-level science products** by default (with provenance and caveats).
- Include provenance (bibcodes, queries, services/endpoints used) in outputs, and allow only
  short excerpts of copyrighted material.

The agent is advisory only: it surfaces comparative, deterministically-ordered candidates with
disclosed caveats; the human makes the final selection. Applies to all output.
